# Анализ проекта Test Okurmen

## 📋 Общая информация

**Название проекта:** Test Okurmen  
**Тип:** Веб-приложение для онлайн-тестирования  
**Технологический стек:** Next.js 16, React 19, TypeScript, Prisma, PostgreSQL, NextAuth  
**Назначение:** Платформа для проведения вступительных тестов в образовательную организацию Okurmen

---

## 🏗️ Архитектура проекта

### Технологии и библиотеки

#### Frontend
- **Next.js 16.1.6** - React-фреймворк с App Router
- **React 19.2.3** - UI библиотека
- **TypeScript 5** - типизация
- **Tailwind CSS 4** - стилизация
- **Lucide React** - иконки
- **React Markdown** - рендеринг Markdown в вопросах

#### Backend
- **Next.js API Routes** - серверная логика
- **Prisma 7.3.0** - ORM для работы с БД
- **PostgreSQL** - база данных
- **NextAuth 5.0.0-beta.30** - аутентификация
- **bcryptjs** - хеширование паролей

#### Дополнительно
- **date-fns** - работа с датами
- **dotenv** - переменные окружения

---

## 📁 Структура проекта

```
test-okurmen/
├── prisma/                    # База данных
│   ├── schema.prisma         # Схема БД
│   └── migrations/           # Миграции
├── public/                   # Статические файлы
│   ├── logo.png
│   ├── logo-for-cta.png
│   └── *.svg
├── scripts/                  # Утилиты
│   └── create-admin.*        # Скрипты создания админа
├── src/
│   ├── app/                  # Next.js App Router
│   │   ├── admin/           # Админ-панель
│   │   ├── api/             # API endpoints
│   │   ├── dashboard/       # Дашборд студента
│   │   ├── login/           # Вход
│   │   ├── register/        # Регистрация
│   │   ├── test/[id]/       # Прохождение теста
│   │   └── my-results/      # Результаты студента
│   ├── components/          # React компоненты
│   │   ├── admin/          # Админские компоненты
│   │   └── ui/             # UI компоненты
│   ├── contexts/           # React Context
│   │   └── LanguageContext.tsx
│   ├── lib/                # Утилиты
│   │   ├── auth.ts         # Настройка NextAuth
│   │   ├── prisma.ts       # Prisma клиент
│   │   └── translations.ts # Переводы
│   ├── types/              # TypeScript типы
│   └── middleware.ts       # Next.js middleware
└── package.json
```

---

## 🗄️ Модель данных (Prisma Schema)

### Основные сущности

#### 1. **User** (Пользователь)
```prisma
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  role      Role     @default(STUDENT)  // ADMIN | STUDENT
  fullName  String
  phone     String
  age       Int
  testAttempts TestAttempt[]
}
```

#### 2. **Test** (Тест)
```prisma
model Test {
  id          String   @id @default(cuid())
  title       String
  description String?
  isActive    Boolean  @default(true)
  questions   Question[]
  testAttempts TestAttempt[]
}
```

#### 3. **Question** (Вопрос)
```prisma
model Question {
  id       String       @id @default(cuid())
  testId   String
  type     QuestionType  // MULTIPLE_CHOICE | TEXT
  question String
  order    Int
  options  Option[]
  answers  Answer[]
}
```

#### 4. **Option** (Вариант ответа)
```prisma
model Option {
  id         String  @id @default(cuid())
  questionId String
  text       String
  isCorrect  Boolean @default(false)
  order      Int
}
```

#### 5. **TestAttempt** (Попытка прохождения)
```prisma
model TestAttempt {
  id          String   @id @default(cuid())
  userId      String
  testId      String
  score       Float?
  completedAt DateTime @default(now())
  answers     Answer[]
  
  @@unique([userId, testId])  // Один тест - одна попытка
}
```

#### 6. **Answer** (Ответ студента)
```prisma
model Answer {
  id               String  @id @default(cuid())
  testAttemptId    String
  questionId       String
  selectedOptionId String?  // Для MULTIPLE_CHOICE
  textAnswer       String?  // Для TEXT
  isCorrect        Boolean?
}
```

---

## 🔐 Система аутентификации

### NextAuth Configuration

**Провайдер:** Credentials (email + password)

**Процесс авторизации:**
1. Пользователь вводит email и пароль
2. Поиск пользователя в БД по email
3. Проверка пароля через bcrypt.compare()
4. Создание JWT токена с ролью и ID
5. Сохранение сессии

**Защита роутов:**
- Middleware проверяет доступ к `/admin/*`
- Админские страницы требуют роль `ADMIN`
- Студенты автоматически перенаправляются на `/dashboard`

---

## 🌐 API Endpoints

### Публичные API

#### Аутентификация
- `POST /api/auth/register` - Регистрация нового студента
- `POST /api/auth/[...nextauth]` - NextAuth endpoints

#### Тесты (для студентов)
- `GET /api/student/available-tests` - Список доступных тестов
- `GET /api/test/[id]` - Получить тест
- `GET /api/test/[id]/attempt` - Проверить наличие попытки
- `POST /api/test/[id]/submit` - Отправить ответы

#### Результаты
- `GET /api/my-results` - Результаты текущего студента

### Админские API (требуют роль ADMIN)

#### Управление тестами
- `POST /api/admin/tests` - Создать тест
- `GET /api/admin/tests/[id]` - Получить тест
- `PATCH /api/admin/tests/[id]` - Обновить тест
- `DELETE /api/admin/tests/[id]` - Удалить тест
- `PUT /api/admin/tests/[id]/questions` - Обновить вопросы

---

## 🎨 Пользовательский интерфейс

### Цветовая схема
- **Основной цвет:** `#111f5e` (темно-синий)
- **Акцентный цвет:** `#f99703` (оранжевый)
- **Фон:** `#f9fafb` (светло-серый)
- **Успех:** `#10b981` (зеленый)

### Основные страницы

#### 1. **Главная страница** (`/`)
- Hero секция с логотипом
- Преимущества платформы (3 карточки)
- О платформе
- Список доступных тестов
- Footer

#### 2. **Регистрация** (`/register`)
- Форма регистрации:
  - ФИО
  - Email
  - Телефон
  - Возраст
  - Пароль
- Автоматический вход после регистрации
- Редирект на тест (если testId в query)

#### 3. **Вход** (`/login`)
- Email и пароль
- Редирект на dashboard или тест

#### 4. **Dashboard студента** (`/dashboard`)
- Приветствие с именем
- Список доступных тестов
- Кнопка "Мои результаты"
- Кнопка выхода

#### 5. **Прохождение теста** (`/test/[id]`)
- Прогресс-бар
- Навигация по вопросам
- Два типа вопросов:
  - Multiple choice (радио-кнопки)
  - Text (textarea)
- Индикатор отвеченных вопросов
- Кнопки "Назад" / "Далее" / "Завершить"

#### 6. **Мои результаты** (`/my-results`)
- Список пройденных тестов
- Оценка в процентах
- Дата прохождения
- Статус проверки

### Админ-панель

#### 7. **Админ логин** (`/admin/login`)
- Отдельная форма входа для админов

#### 8. **Админ dashboard** (`/admin/dashboard`)
- Статистика:
  - Количество тестов
  - Количество студентов
  - Количество попыток

#### 9. **Управление тестами** (`/admin/tests`)
- Список всех тестов
- Создание нового теста
- Редактирование теста
- Удаление теста
- Активация/деактивация

#### 10. **Создание теста** (`/admin/tests/create`)
- Название и описание
- Добавление вопросов
- Два типа вопросов
- Порядок вопросов

#### 11. **Редактирование теста** (`/admin/tests/[id]/edit`)
- Изменение информации о тесте
- Редактирование вопросов
- Изменение вариантов ответов

#### 12. **Результаты** (`/admin/results`)
- Список всех попыток
- Фильтрация по тестам
- Просмотр ответов студентов

---

## 🌍 Мультиязычность

### Поддерживаемые языки
- **Русский** (ru) - по умолчанию
- **Кыргызский** (ky)

### Реализация
- React Context (`LanguageContext`)
- LocalStorage для сохранения выбора
- Компонент `LanguageSwitcher`
- Файл переводов: `src/lib/translations.ts`

### Переведенные элементы
- Все UI тексты
- Кнопки и формы
- Сообщения об ошибках
- Навигация

---

## 🔒 Безопасность

### Реализованные меры

1. **Хеширование паролей**
   - bcryptjs для хеширования
   - Пароли никогда не хранятся в открытом виде

2. **JWT токены**
   - NextAuth использует JWT для сессий
   - Токены содержат роль и ID пользователя

3. **Middleware защита**
   - Проверка доступа к админским роутам
   - Автоматический редирект неавторизованных

4. **Ограничение попыток**
   - Уникальный constraint `[userId, testId]`
   - Студент может пройти тест только один раз

5. **Валидация на сервере**
   - Проверка данных в API routes
   - Проверка прав доступа

---

## 📊 Бизнес-логика

### Процесс прохождения теста

1. **Регистрация/Вход**
   - Студент регистрируется или входит
   - Создается User с ролью STUDENT

2. **Выбор теста**
   - Студент видит активные тесты
   - Проверка: не проходил ли уже этот тест

3. **Прохождение**
   - Загрузка вопросов
   - Ответы сохраняются локально
   - Навигация между вопросами

4. **Отправка**
   - Проверка: все ли вопросы отвечены
   - Создание TestAttempt
   - Создание Answer для каждого вопроса
   - Автоматическая проверка MULTIPLE_CHOICE
   - Подсчет score

5. **Результаты**
   - Студент видит свой score
   - Админ видит все попытки

### Система оценивания

- **MULTIPLE_CHOICE:** автоматическая проверка
  - Сравнение selectedOptionId с isCorrect
  - isCorrect = true/false

- **TEXT:** ручная проверка
  - isCorrect = null (до проверки)
  - Админ проверяет вручную

- **Score:** процент правильных ответов
  - `(correctAnswers / totalQuestions) * 100`

---

## 🛠️ Утилиты и скрипты

### Создание админа

Три варианта скрипта:
1. `create-admin.ts` - TypeScript версия
2. `create-admin.mjs` - ES модули
3. `create-admin-direct.mjs` - Прямое подключение к БД

**Использование:**
```bash
npm run tsx scripts/create-admin.ts
```

---

## 🚀 Развертывание

### Переменные окружения

```env
DATABASE_URL="postgresql://..."
NEXTAUTH_SECRET="..."
NEXTAUTH_URL="http://localhost:3000"
```

### Команды

```bash
# Установка зависимостей
npm install

# Миграции БД
npx prisma migrate dev

# Генерация Prisma Client
npx prisma generate

# Создание админа
npm run tsx scripts/create-admin.ts

# Разработка
npm run dev

# Продакшн
npm run build
npm start
```

---

## 📈 Возможности для улучшения

### Функциональные
1. Таймер для тестов
2. Случайный порядок вопросов
3. Экспорт результатов в Excel/PDF
4. Email уведомления
5. Система комментариев к ответам
6. Категории тестов
7. Сложность вопросов
8. Банк вопросов
9. Повторное прохождение тестов
10. Детальная статистика для админа

### Технические
1. Оптимизация изображений (next/image)
2. Кеширование запросов
3. Pagination для списков
4. Поиск и фильтрация
5. Rate limiting для API
6. Логирование действий
7. Backup системы
8. Мониторинг ошибок (Sentry)
9. Тесты (Jest, Playwright)
10. CI/CD pipeline

### UX/UI
1. Темная тема
2. Адаптивность для мобильных
3. Анимации переходов
4. Прогресс сохранения
5. Подтверждение действий
6. Breadcrumbs навигация
7. Keyboard shortcuts
8. Accessibility улучшения

---

## 🎯 Ключевые особенности проекта

### Сильные стороны

✅ **Современный стек:** Next.js 16 + React 19  
✅ **Типобезопасность:** TypeScript + Prisma  
✅ **Мультиязычность:** Русский и Кыргызский  
✅ **Безопасность:** NextAuth + bcrypt  
✅ **Гибкость:** Два типа вопросов  
✅ **UX:** Интуитивный интерфейс  
✅ **Масштабируемость:** Модульная архитектура  

### Архитектурные решения

1. **App Router:** Использование новой архитектуры Next.js
2. **Server Components:** Оптимизация производительности
3. **API Routes:** Централизованная бизнес-логика
4. **Prisma ORM:** Типобезопасная работа с БД
5. **Context API:** Управление состоянием языка
6. **Middleware:** Защита роутов на уровне сервера

---

## 📝 Заключение

**Test Okurmen** - это полнофункциональная платформа для онлайн-тестирования с четким разделением ролей (админ/студент), мультиязычностью и современным технологическим стеком. Проект демонстрирует правильное использование Next.js 16, Prisma, NextAuth и следует best practices разработки веб-приложений.

Архитектура проекта позволяет легко масштабировать функциональность, добавлять новые типы вопросов, интегрировать дополнительные сервисы и расширять возможности платформы.
